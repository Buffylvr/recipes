# Nutritional Information

Nutrition facts (label photos or notes) for specific ingredient brands/products,
for when the exact numbers matter for a recipe's per-serving macros.

Name files after the ingredient, matching `pictures/` and `locations/`, e.g.
`tempeh.md` or `tempeh.jpg`.
